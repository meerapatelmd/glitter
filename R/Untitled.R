# fns <- list.files("R",
#                   full.names = TRUE)
#
# for (file in fns) {
#         readfile <- read_lines(file = file)
#         if (any(grepl("push_wd()", readfile))) {
#
#                 secretary::press_enter()
#         }
# }
